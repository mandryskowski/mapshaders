# SPDX-License-Identifier: Unlicense

message("Cmake build type is ${CMAKE_BUILD_TYPE}")

file(GLOB_RECURSE MAPSHADERS_SOURCES CONFIGURE_DEPENDS
    "*.cpp**"
    PARENT_SCOPE
)
file(GLOB_RECURSE MAPSHADERS_HEADERS CONFIGURE_DEPENDS
    "*.h**"
    PARENT_SCOPE
)
# Elevate to parent scope
set(MAPSHADERS_SOURCES ${MAPSHADERS_SOURCES} PARENT_SCOPE)
set(MAPSHADERS_HEADERS ${MAPSHADERS_HEADERS} PARENT_SCOPE)

message(${MAPSHADERS_SOURCES})
