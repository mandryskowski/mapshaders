file(GLOB_RECURSE WORLDSHADERS_SOURCES CONFIGURE_DEPENDS
    "*.cpp**"
    PARENT_SCOPE
)
file(GLOB_RECURSE WORLDSHADERS_HEADERS CONFIGURE_DEPENDS
    "*.h**"
    PARENT_SCOPE
)
# Elevate to parent scope
set(MAPSHADERS_SOURCES ${MAPSHADERS_SOURCES} ${WORLDSHADERS_SOURCES} PARENT_SCOPE)
set(MAPSHADERS_HEADERS ${MAPSHADERS_HEADERS} ${WORLDSHADERS_HEADERS} PARENT_SCOPE)

message(${MAPSHADERS_SOURCES})
